# Talos Secrets Configuration
# Copy this file to secrets.env and fill in your values
# DO NOT commit secrets.env to git!

# Talos Secrets Bundle (generated by talosctl gen secrets)
# This will be auto-generated by build-talos-configs.sh
# Format: Base64-encoded secrets.yaml content
TALOS_SECRETS_BUNDLE=''

# Disk Encryption Passphrase (optional, for LUKS encryption)
# If empty, disk encryption will be disabled
# Generate with: openssl rand -base64 32
DISK_ENCRYPTION_KEY=''

# Longhorn data disk (per host). Use by-id for stability.
# Find IDs via: talosctl --insecure --nodes <IP> ls /dev/disk/by-id/
LONGHORN_DISK_HOST01='/dev/disk/by-id/REPLACE_ME'
LONGHORN_DISK_HOST02='/dev/disk/by-id/REPLACE_ME'
LONGHORN_DISK_HOST03='/dev/disk/by-id/REPLACE_ME'

# VIP (Virtual IP) for Kubernetes API Server
# This IP must be in the same subnet as your nodes (10.0.100.0/24)
VIP_ADDRESS='10.0.100.111'

# GitHub Repository for GitOps (for ArgoCD bootstrap)
GITOPS_REPO='https://github.com/YOUR_USERNAME/k8s-homelab-gitops.git'
GITOPS_BRANCH='main'

# Optional: Custom DNS Servers (comma-separated)
# Leave empty to use DHCP-provided DNS
DNS_SERVERS='8.8.8.8,8.8.4.4'

# Optional: NTP Servers (comma-separated)
# Leave empty to use default pool.ntp.org
NTP_SERVERS='pool.ntp.org'
