# Secrets Configuration for FCOS Butane Configs
# Copy this file to secrets.env and fill in your values
# secrets.env is gitignored and will NOT be committed

# SSH Public Key (for passwordless login)
# Generate with: ssh-keygen -t rsa -b 4096 -C "homelab-cluster"
# Get key with: cat ~/.ssh/id_rsa.pub
SSH_PUBLIC_KEY='ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQC... your-email@example.com'

# Password Hash (for console/emergency login)
# Generate with: mkpasswd -m sha-512
# On macOS: brew install mkpasswd
# On Ubuntu/Debian: sudo apt install whois
PASSWORD_HASH='$6$rounds=4096$saltsaltlettuce$Lp/FV.2oOgew7GbM6Nr8KMGMBn7iFM0x9ZwLqNau6vzxZIqc3p3Lc3N.XKQP5../tNuM1QN/L0jG3rkgBklDd.'

# RKE2 Join Token (get from bootstrap node after installation)
# On host03: sudo cat /var/lib/rancher/rke2/server/node-token
# Leave empty for initial bootstrap, then fill in and regenerate join configs
JOIN_TOKEN=''
